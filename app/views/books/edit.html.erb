<h1>編集画面</h1>
<!-- form_with部分 -->
<%= form_with model:@book, local: true do |f| %>
  <p>
  <strong>TItle:</strong><%= f.text_field :title %>
</p>
  <p>
  <strong>Body:</strong><%= f.text_area :body %>
  </p>
  <%= f.submit '保存' %>

<% end %>